services:
  elasticsearch:
    image: elasticsearch:7.17.15
    privileged: true
    environment:
      - "cluster.name=elasticsearch" #设置集群名称为elasticsearch
      - "discovery.type=single-node" #以单一节点模式启动
      - "ES_JAVA_OPTS=-Xms512m -Xmx1096m" #设置使用jvm内存大小
      - bootstrap.memory_lock=true
      - "xpack.security.enabled=true" # 启用 Elasticsearch 安全功能
      - "bootstrap.system_call_filter=false" # 禁用系统调用过滤器
    volumes:
      - ./data/plugins:/usr/local/dockercompose/elasticsearch/plugins #插件文件挂载
      - ./data/data:/usr/local/dockercompose/elasticsearch/data:rw #数据文件挂载
      - ./data/logs:/usr/local/dockercompose/elasticsearch/logs:rw
    ports:
      - 9200:9200
      - 9300:9300
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 1000M
        reservations:
          memory: 200M
  kibana:
    image: kibana:7.17.15
    container_name: kibana
    depends_on:
      - elasticsearch #kibana在elasticsearch启动之后再启动
    environment:
      ELASTICSEARCH_HOSTS: http://elasticsearch:9200 #设置访问elasticsearch的地址
      I18N_LOCALE: zh-CN
    ports:
      - 5601:5601
